version: "3.8"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: my-laravel-app
        restart: unless-stopped
        ports:
            - "8080:80"
        environment:
            DB_HOST: db
            DB_DATABASE: study-tracer
            DB_USERNAME: root
            DB_PASSWORD: secret
        volumes:
            - .:/var/www/html
        networks:
            - app-network

    db:
        image: mysql:8.0
        container_name: my-mysql-db
        restart: unless-stopped
        environment:
            MYSQL_ROOT_PASSWORD: secret
            MYSQL_DATABASE: study-tracer
            MYSQL_USER: personal
            MYSQL_PASSWORD: secret
        volumes:
            - mysql-data:/var/lib/mysql
        networks:
            - app-network

volumes:
    mysql-data:

networks:
    app-network:
        driver: bridge
